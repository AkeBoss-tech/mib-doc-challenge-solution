# 130-capability autoresearch cycle

## Objective

Raise the official public-train score from the validated `119.462216`
candidate toward `130`, while preserving zero catastrophic false approvals and
the offline Docker contract.

## Provenance boundary

This cycle uses only the repository's original implementation, rendered public
pixels, public training labels for measurement, official challenge rules, and
official OCR/documentation. Public participant memos informed only the broad
research priorities (orientation, selective fallback OCR, evidence resolution,
and source-aware calibration). No participant code, thresholds, layouts, tests,
predictions, case rules, or artifacts are used.

## Incumbent

- Total: `119.462216 / 150`
- Extraction: `39.245556 / 50`
- Classification: `64.690000 / 80`
- Calibration: `15.526660 / 20`
- Catastrophic false approvals: `0`
- Valid records: `1000 / 1000`

The Docker-native learned-routing candidate has a derived score of
`120.465540`, but its full Docker gate was interrupted and it is not yet an
accepted checkpoint.

## Loop order

1. Complete the Docker-native decision-model full gate.
2. Build an exact-engine field failure ledger.
3. Measure generic orientation and label-geometry readers on failing fields.
4. Add selective second-engine OCR only if unresolved-field yield justifies it.
5. Improve evidence reconciliation, then approval recovery, then calibration.

Every promoted lever must improve grouped evaluation, pass unit tests, preserve
zero catastrophic false approvals, and clear the full Docker gate.

## Validated checkpoint and current candidate

- Docker-native decision routing cleared the full official gate at
  `120.465540`, with `1000 / 1000` valid rows and zero catastrophic false
  approvals.
- The first orientation implementation was rejected because it allowed retry
  OCR to overwrite valid native values.
- Selective orientation merge was measured against a same-code disabled
  control on an 80-case development/holdout cohort. Development had no field
  regressions and recovered one risk flag. Holdout improved eight fields,
  recovered one correct approval, and had zero catastrophic false approvals.
  It remains pending a full Docker gate.
- The current full-Docker checkpoint is `125.593785` with `1000 / 1000` valid
  records and zero catastrophic false approvals. Applicant-bound sponsor prose
  and exact visible manual findings changed 45 names, 26 purposes, 15 sponsors,
  13 visa classes, and 6 adjudications; every change was correct across the
  development/holdout split, with no formerly correct value displaced.
- The next deterministic evidence stack cleared the full Docker gate at
  `126.381740`, again with `1000 / 1000` valid rows and zero catastrophic false
  approvals. Exact manual corrections, output-only date consensus, exact
  adverse flags, and a paid `SPN-4040` denial conjunction improved extraction,
  classification, and calibration without displacing a correct field.
